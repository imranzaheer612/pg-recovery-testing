PGHOME="/home/imran/Desktop/work/pg/installs/pgtest/bin"
TEST_DIR="/media/imran/drive-ext4-free/linux-tmp/testing"

# default workloads incase custome scripts forced to use with pgbench
WORKLOAD="sql/workloads/updates.sql"
DB_INIT="sql/primary-init.sql"

# pgbench settings used for generating wal
WORKLOAD_DURATION=400
CLIENTS=20
THREADS=20

# pgbench int settings
SCALE_FACTOR=300
FILL_FACTOR=100

#
# Linux perf need to be set on the system before turning this defautl to on.
# Run the following command if not set
#
# sudo sysctl kernel.kptr_restrict=0
# sudo sysctl kernel.perf_event_paranoid=-1 
#
: "${PERF_PROFILING:=off}"
